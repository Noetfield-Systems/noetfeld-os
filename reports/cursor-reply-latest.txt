---
agent_tag: nf-local-repo-agent
agent_display: "[NF-LOCAL-REPO-AGENT]"
authored_at: "2026-07-06"
doc_id: gov-sandbox-ship-closeout-92
---

> **Authored by:** [NF-LOCAL-REPO-AGENT] — 2026-07-06

## What shipped (plain English)

Governance sandbox is live end-to-end: Railway hosts the Trust Ledger workspace, and www.noetfield.com/workspace proxies to it through Cloudflare Pages middleware. PR #91 added the proxy scaffold (fail-closed when off). PR #92 fixed Railway so /workspace serves the workspace UI instead of redirecting away, and turned the proxy on in config.

Live checks passed: Railway API /health 200, Railway /workspace 200, www /workspace shows Trust Ledger Workspace + _next assets, verify-www-live-sandbox.sh PASS.

## PRs merged

| PR | Branch | Summary |
|----|--------|---------|
| #90 | fix/www-tech-only | www tech lane, playground v50 restore, audit machine |
| #91 | fix/www-gov-proxy-middleware | Pages middleware gov proxy (fail-closed off) |
| #92 | fix/gov-sandbox-workspace | Railway /workspace fix + proxy enabled |

## Follow-up PR (open next)

**Branch:** `fix/gov-deploy-hardening` (from main after #92)

**Why:** Deploy scripts failed on timing/URL resolution even though production was fine.

**Changes:**
- `scripts/deploy-gov-sandbox-railway.sh` — resolve URLs via `railway domain list --json` (status JSON has no URL field), retry loop, fallback to cached `data/nf-www-gov-proxy-v1.json`
- `scripts/deploy-www-cloudflare.sh` — wait for preview `/api/health` 200 before post-deploy verify (fixes false FAIL on cold preview)

**Verify after merge:**
```bash
bash scripts/verify-www-live-sandbox.sh
# optional full redeploy smoke:
bash scripts/deploy-gov-sandbox-railway.sh   # should print API + WEB URLs, no exit 2
```

## Next move options (pick one)

1. **Merge deploy-hardening PR** — closes the script gaps from this batch; no user-facing change.
2. **N-P8 sticky CTA** — portfolio pf-0074 pilot apply CTA on gate + trust-brief (plan.json done marker; ship if not live).
3. **Apex DNS** — `noetfield.com` (non-www) still 404; separate Cloudflare redirect/DNS pass.
4. **Factory round 16** — portfolio 260/300; status pages + registry sync already marked done — pick next pf-* from `make pick-wise`.

## Live surfaces (disk)

- product_now_line: no pending next_tasks
- email_send_defer_line: email-defer · ON · main=5/5 · sites=RED · email=AFTER_MAIN · lift=YES

## Commands for next agent

```bash
make nf-onboard
git checkout main && git pull
git checkout -b fix/gov-deploy-hardening   # or merge PR if already open
bash scripts/verify-www-live-sandbox.sh
```

---
agent_tag: nf-local-repo-agent
agent_display: "[NF-LOCAL-REPO-AGENT]"
reporter: cursor
reported_at: "2026-07-06T14:55:00Z"
