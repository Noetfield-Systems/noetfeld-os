# Retired 2026-07-08 — replaced by noos-stack-health-receipt.yml (live rollup).
# Archived from .github/workflows/ — do not re-enable.
name: "NOOS Cross-Repo Health Orchestrator (RETIRED)"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED: use noos-stack-health-receipt.yml"
          echo "Receipt: receipts/proof/noos-stack-automation-health-v1.json"
          exit 1
