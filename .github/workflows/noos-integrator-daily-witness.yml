# NOOS GHA secondary witness — integrator daily control panel (ICL-D01–D13).
name: NOOS integrator daily witness

on:
  workflow_dispatch:
  schedule:
    - cron: "0 7,13,19 * * *"

permissions:
  contents: read
  actions: read

concurrency:
  group: noos-integrator-daily-witness-${{ github.ref }}
  cancel-in-progress: false

jobs:
  witness:
    name: Integrator daily checklist
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      actions: read
    env:
      NOOS_GHA_WITNESS: "1"
      NOOS_MOTOR_WITNESS_MOTORS_ONLY: "1"
      GITHUB_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CF_NOETFIELD_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      NOETFIELD_SUPABASE_URL: ${{ secrets.NOETFIELD_SUPABASE_URL }}
      NOETFIELD_SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.NOETFIELD_SUPABASE_SERVICE_ROLE_KEY }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Bootstrap autorun repair (liveness + loop cycles)
        run: python3 scripts/noos_integrator_repair_autorun_v1.py --json

      - name: Integrator daily checklist
        run: |
          set -euo pipefail
          rc=0
          python3 scripts/noos_integrator_daily_checklist_v1.py --write-receipt --json --witness-gha || rc=$?
          echo "WITNESS_RC=${rc}" >> "$GITHUB_ENV"

      - name: Commit witness receipt to main
        if: always()
        run: |
          set -euo pipefail
          git config user.name "noos-witness-bot"
          git config user.email "noos-witness-bot@users.noreply.github.com"
          git add receipts/proof/noos-integrator-daily-checklist-v1.json
          if git diff --cached --quiet; then
            echo "No receipt change to commit."
          else
            git commit -m "witness: noos-integrator-daily-witness receipt $(date -u +%Y-%m-%dT%H:%M:%SZ) [skip ci]"
            for attempt in 1 2 3; do
              if git pull --rebase --autostash origin main && git push origin HEAD:main; then
                echo "Receipt pushed to main (attempt ${attempt})."
                break
              fi
              if [ "${attempt}" -eq 3 ]; then
                echo "Receipt push failed after 3 attempts." >&2
                exit 1
              fi
              sleep 7
            done
          fi

      - name: Upload daily checklist receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: noos-integrator-daily-checklist-v1
          path: receipts/proof/noos-integrator-daily-checklist-v1.json
          retention-days: 30
          if-no-files-found: warn

      - name: Fail run on red witness verdict
        if: always()
        run: |
          rc="${WITNESS_RC:-1}"
          echo "Witness exit code: ${rc}"
          exit "${rc}"
